using System.IO;
using Backups.Entities;
using Backups.Services;

namespace Backups
{
    internal class Program
    {
        private static void Main()
        {
            // var service = new BackupsService();
            // string testDirectoryPath = @"/home/vaskers5/projects/oop/Backups/TestDirectory";
            // var job1 = service.AddNewJob(testDirectoryPath + "/job-1", BackupAlgorithms.Split);
            //
            // service.AddFileToJob(job1, testDirectoryPath + "/a.txt");
            // service.AddFileToJob(job1, testDirectoryPath + "/b.txt");
            // service.AddFileToJob(job1, testDirectoryPath + "/c.txt");
            // try
            // {
            //     job1.CreateNewRestorePoint();
            // }
            // catch
            // {
            //     Directory.Delete(testDirectoryPath + "/job-1");
            // }
        }
    }
}
